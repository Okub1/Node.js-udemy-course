extends layouts/main-layout.pug

block content 
    main 
        form(action="/admin/add-product" method="POST")
            label(for="title") Name
            input(type="text" name="title")
            button(type="submit" disabled) Add product